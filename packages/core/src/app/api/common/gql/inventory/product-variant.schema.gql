type ProductVariant implements Node {
  id: ID!
  createdAt: Date!
  updatedAt: Date!
  sku: String!
  price: Int!
  comparisonPrice: Int
  costPerUnit: Int!
  weight: Int
  stock: Int!
  enabled: Boolean!
}