# @vendyx/common
The `@vendyx/common` module serves as a foundational and shared component within the Vendyx software ecosystem, providing essential functionalities and utilities that are commonly used across all modules.

## Usage
```ts
import { Admin } from '@vendyx/common';
```