scalar Date
scalar ID

interface Node {
  id: ID!
  createdAt: Date!
  updatedAt: Date!
}

interface List {
  items: [Node]!
  totalItems: Int!
}

type PaginatedListInput {
  skip: Int
  first: Int
  last: Int
}
