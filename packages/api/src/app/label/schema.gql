type Label {
  id: String!
  createdAt: String!
  updatedAt: String!
  name: String!

  values: [LabelValues]!
}

type LabelValues {
  id: String!
  createdAt: String!
  updatedAt: String!
  value: String!

  label: Label!
}

type Query {
  label(id: ID!): Label
  labels: [Label]
}
