type Collection implements Node {
  id: ID!
  createdAt: Date!
  updatedAt: Date!
  name: String!
  slug: String!
  description: String
  enabled: Boolean!

  products: [ProductList]!
  assets: [AssetList]!
}

type CollectionList implements List {
  items: [Collection]!
  count: Int!
}
