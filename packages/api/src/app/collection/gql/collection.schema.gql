type Collection implements Node {
  id: ID!
  createdAt: Date!
  updatedAt: Date!
  name: String!
  slug: String!
  description: String
  enable: Boolean!

  products: [ProductList]!
  assets: [AssetList]!
}

type CollectionList implements List {
  items: [Collection]!
  totalItems: Int!
}
